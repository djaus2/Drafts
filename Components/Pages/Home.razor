@page "/"
@rendermode @(new InteractiveServerRenderMode(prerender: false))
@* @attribute [Microsoft.AspNetCore.Authorization.Authorize(Roles = "Player,Admin")]
 *@
@inject NavigationManager Nav
<PageTitle>Checkers online</PageTitle>

<br />

<p>Play Drafts or Checkers with a friend online</p>

<button @onclick="Start">Start a Game</button>

<br />

<img src="Images/TheBoard.png" width="400"/>

@code{
    private void Start()
    {
        Nav.NavigateTo($"/login");
    }
}