# Requests (2026-03-08)

- **Global buttons**: Add a global rainbow-themed style for all `button` elements (hover/active/disabled) while allowing inline overrides.
- **Stop dynamic rainbow**: Make all “rainbow” styling non-animated/static like the main Checkers heading.
- **Button text contrast**: Improve contrast; use a purple text color and make font weight bold.
- **Fade button background**: Soften button backgrounds by ~50%.
- **Match board colors**: Change button colors to match board colors (white/beige/brown/beige/white).
- **Reduce white ends**: Adjust the button gradient so the white at left/right is smaller and non-white spreads more.
- **Color math**: Ask for ARGB values / midpoint color between `#7B5E3B` and `#EEE8D5`.
- **/Draughts headings**: Make the “Chat” and “Send” headings on `/Draughts` non-dynamic (stop animating).
- **Web.config creds**: Ask about fixing username/password via `web.config`.
- **Azure publish creds local-only**: Store Azure publishing credentials locally only (not in repository).
- **MSDeploy publishing issues**: Debug why the saved password wasn’t being used; resolve publishing errors (Basic auth vs MFA/AAD).
- **Locate publish profiles**: Ask where Visual Studio publish profiles are stored and adjust profiles to reduce warnings.
- **Board image text**: Add diagonal red “Lets play!” text to `wwwroot/Images/TheBoard.png`.
- **Board image text size**: Increase the overlay font size by ~50%.
- **Structured conversation**: Ask how to capture the conversation in a structured manner; request “Session notes” format.
- **Yesterday notes**: Generate a similar notes file for yesterday.
- **List of requests**: Ask for a Markdown list of today’s requests.
