# Requests (2026-03-07)

These are inferred from the git commits made on 2026-03-07 (since we don’t have the full chat transcript for that day).

- **Implement Entrapment Mode**
  - Add a new “Entrapment Mode” rules variant.
  - Make it configurable via settings.
  - Persist the setting (so it survives restarts).
  - Expose/configure it via the Admin page.

- **Implement double-jump / multi-jump gameplay**
  - Support multi-capture sequences (double jumps).
  - Update turn/flow logic so a player can continue jumping when required/allowed.
  - Persist any related settings.
  - Update UI/game component to support the new move flow.
  - Expose/configure any related options via the Admin page.

- **Update project/config wiring to support the above**
  - Ensure new settings are included and seeded appropriately.
  - Ensure build/publish artifacts reflect the new features.
